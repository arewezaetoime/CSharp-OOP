

namespace Polymorphism
{
    internal class MathOperations
    {
        internal int Add(int x, int y) => x + y;
        internal double Add(double x, double y, double z) => x + y + z;
        internal decimal Add(decimal x, decimal y, decimal z) => x + y + z;
    }
}
